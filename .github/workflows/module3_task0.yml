name: module_3_task0
on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"

jobs:
  test_schedule:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone repository
        uses: actions/checkout@v2

      - name: Go into correct directory and execute make help
        run: cd module_3_task0 make help
